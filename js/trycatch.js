'use strict'



try {                       // intentamos ejecutar codigo susceptible de errores
    var year = 2020;
    alert(yea);   
} 

catch (error) {             // Capturamos error si se produce y decidimos que hacer
    alert("Ha ocurrido un error!")    
}
